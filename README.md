Requires: opencv, scikit-learn, numpy, matplotlib

Before running the script, goto ./training/ to train some classifiers named class.pkl

Usage: to run the whole testing process, type

	run.py <test image path>

e.g
	run.py ./ir/
